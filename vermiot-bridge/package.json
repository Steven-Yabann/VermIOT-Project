{
  "name": "vermiot-bridge",
  "version": "1.0.0",
  "description": "MQTT to Firebase bridge for VermIoT — ICS 4111, Group 4E",
  "main": "bridge.js",
  "scripts": {
    "start": "node --use-system-ca bridge.js",
    "dev": "nodemon --exec \"node --use-system-ca\" bridge.js"
  },
  "dependencies": {
    "dotenv": "^16.4.5",
    "firebase-admin": "^12.2.0",
    "mqtt": "^5.6.1",
    "nodemailer": "^9.0.3"
  },
  "devDependencies": {
    "nodemon": "^3.1.4"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
